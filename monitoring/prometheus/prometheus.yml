global:
  scrape_interval:     10s
  evaluation_interval: 10s
  external_labels:
    monitor: pipeline-modeling

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: pipeline_api
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
    scrape_timeout: 8s
